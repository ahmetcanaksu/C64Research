[package]
name = "c64-kernal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Readable Rust reimplementations of C64 KERNAL routines — reference material, not a runtime dependency."

[lib]
name = "kernal"

[dependencies]
