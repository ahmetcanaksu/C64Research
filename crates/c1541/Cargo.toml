[package]
name = "c1541"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "1541 disk drive: a bootable machine (CPU + RAM + ROM + 2 VIAs) plus hand-ported DOS routines."

[dependencies]
mos6502 = { path = "../mos6502" }
via6522 = { path = "../via6522" }
iec = { path = "../iec" }
d64 = { path = "../d64" }
gcr = { path = "../gcr" }
